# Phase 0 spike — frame

Instance of the `learn-ilt` preset at `learn/frame-presets/learn-ilt/FRAME.md`. Purpose: prove
an on-brand Learn composition (warm-paper ground, ink text, gradient field + dither) with a
`cut-the-curve` seam passes `hyperframes lint` + `check` (incl. WCAG AA contrast) inside the
fork, self-contained. See the preset for the normative tokens.

- Scene 1: title spotlight on `hero-swoosh`, Segoe display ink.
- Seam: cut-the-curve, LEFT (the current).
- Scene 2: a branded bar+line data viz on `content-wash`, all text ink, accents on marks only.
