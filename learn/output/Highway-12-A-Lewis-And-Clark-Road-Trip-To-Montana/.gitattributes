# Generated media images for this video are large binaries → LFS. In scope for the video branch
# (root .gitattributes already LFS-tracks *.wav, *.mp4, and assets/grounds/*.png).
assets/media/**/*.png filter=lfs diff=lfs merge=lfs -text
